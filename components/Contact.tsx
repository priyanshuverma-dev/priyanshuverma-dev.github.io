"use client";

import { Widget } from "@typeform/embed-react";
import React from "react";

export default function ContactForm() {
  return (
    <Widget
      id="cmOmloEL"
      className="h-[500px] mt-4 bg-primary-foreground"
      lazy
      hideHeaders
      hideFooter
    />
  );
}
